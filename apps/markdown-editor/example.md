# Collaborative Markdown

This is one shared document for trying ordinary Markdown editing, selection,
composition, wrapping, and Unicode text.

## A small plan

- [x] Open the document
- [ ] Edit from two replicas
- [ ] Watch the changes converge

> Durable content belongs to the document. Selection, composition, and cursor
> presence belong to each client.

| Person | Status | Note |
| --- | --- | --- |
| Aroha | editing | Kia ora 👋 |
| Ren | reviewing | 日本語も編集できます |
| Noor | ready | مرحبًا |

Inline `code`, **strong text**, _emphasis_, and a
[link](https://example.com/) should remain ordinary text while editing.

```ts
const message = 'Automerge changes, not replacement documents';
```

The final paragraph is deliberately long enough to wrap in a narrow pane, so
clicking, dragging, and composing text can be tried across visual lines without
turning the example into a synthetic stress fixture.
